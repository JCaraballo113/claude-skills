---
name: grill-unknowns
description: A relentless interview to find the gap between the user's plan and the codebase's real constraints — their unknowns — before, during, or after implementation.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/john-superpowers:finding-unknowns` skill.

If a skill in [skill.deps.json](./skill.deps.json) isn't installed, prompt the user to run its install command first.
